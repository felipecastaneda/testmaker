rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to images, but keep uploads secure (or allow for prototype)
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null || true; // Allowing for prototype
    }
  }
}
